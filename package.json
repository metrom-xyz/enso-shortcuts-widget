{
  "name": "main",
  "version": "1.0.0",
  "scripts": {
    "dev:app": "pnpm --filter widget-app run dev",
    "dev:widget": "pnpm --filter @metrom-xyz/enso-shortcuts-widget run dev",
    "build:app": "pnpm --filter widget-app run build",
    "build:widget": "pnpm --filter @metrom-xyz/enso-shortcuts-widget run build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "wagmi": "2.14.12"
  },
  "pnpm": {
    "overrides": {
      "react": "^18.2.0 || ^19.0.0",
      "react-dom": "^18.2.0 || ^19.0.0"
    }
  }
}
